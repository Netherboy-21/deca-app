<%= render "users/header" %>

<h1>New category</h1>

<%= form_with model: [ @user, @user.categories.build ] do |form| %>
  <div>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>